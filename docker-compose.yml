version: '3.8'

services:
  tutor-bot:
    build: .
    container_name: telegram-tutor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
